#apiVersion: v1
#kind: Secret
#metadata:
#  name: {{ template "redash.fullname" . }}
#  labels:
#    app: {{ template "redash.fullname" . }}
#    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
#    release: "{{ .Release.Name }}"
#    heritage: "{{ .Release.Service }}"
#type: Opaque
#data:
#  redash-database-url: "postgresql://{{ .Values.db.user }}:{{ .Values.db.pass }}@{{ .Values.db.url }}/{{- .Values.db.name }}:{{ .Values.db.port }}?sslmode=disable"
